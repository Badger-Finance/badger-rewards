apiVersion: batch/v1beta1
kind: CronJob
metadata:
    name: approve-avax-cycle-bot
    labels:
        app: approve-avax-cycle-bot
spec:
    schedule: '15 */2 * * *'
    startingDeadlineSeconds: 180
    suspend: false
    jobTemplate:
        spec:
            template:
                spec:
                    serviceAccountName: approve-avax-cycle-bot
                    containers:
                        - name: approve-avax-cycle-bot
                          image: IMAGE_NAME
                          imagePullPolicy: IfNotPresent
                          command: ['python', '-m', 'scripts.rewards.avax.approve_avax']
                          env:
                              - name: LOG_LEVEL
                                value: 'info'
                              - name: ENV
                                value: 'PROD'
                              - name: KUBE
                                value: 'True'
                              - name: 'FIX_CYCLE'
                                value: 'False'
                    restartPolicy: OnFailure
