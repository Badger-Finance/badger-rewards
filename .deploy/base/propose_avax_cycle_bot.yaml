apiVersion: batch/v1beta1
kind: CronJob
metadata:
    name: propose-avax-cycle-bot
    labels:
        app: propose-avax-cycle-bot
spec:
    schedule: '5 */2 * * *'
    startingDeadlineSeconds: 180
    suspend: false
    jobTemplate:
        spec:
            template:
                spec:
                    serviceAccountName: propose-avax-cycle-bot
                    containers:
                        - name: propose-avax-cycle-bot
                          image: IMAGE_NAME
                          imagePullPolicy: IfNotPresent
                          command: ['python', '-m', 'scripts.rewards.avax.propose_avax']
                          env:
                              - name: LOG_LEVEL
                                value: 'info'
                              - name: ENV
                                value: 'PROD'
                              - name: KUBE
                                value: 'True'
                              - name: FIX_CYCLE
                                value: 'False'
                    restartPolicy: OnFailure

