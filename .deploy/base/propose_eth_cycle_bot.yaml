apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: propose-eth-cycle-bot
  labels:
    app: propose-eth-cycle-bot
spec:
  schedule: "56 14 * * *"
  startingDeadlineSeconds: 180
  suspend: false
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: propose-eth-cycle-bot
          containers:
          - name: propose-eth-cycle-bot
            image: IMAGE_NAME
            imagePullPolicy: IfNotPresent
            command: ["python", "-m", "scripts.rewards.eth.retroactive.fix_eth_rewards.fix_eth_rewards.propose"]
            env:
              - name: LOG_LEVEL
                value: 'info'
              - name: TEST
                value: 'False'
              - name: KUBE
                value: 'True'
          restartPolicy: OnFailure
